<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="QuickSearch" Height="450" Width="800"
        Background="Wheat">
        
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Label 
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Grid.Row="0"
            Margin="0,0,0,0">
            Filter files
        </Label>
        <TextBox
            x:Name="SearchBox"
            TextChanged="SearchBox_TextChanged"
            VerticalAlignment="Top"
            Grid.Row="1"/>

        <ListBox
            Grid.Column="0"
            Grid.Row="2"
            VerticalAlignment="Stretch"
            x:Name="FoundFiles" 
            ItemsSource="{Binding Files}" 
            SelectedItem="{Binding SelectedFile, Mode=TwoWay}"
            DisplayMemberPath="Name" 
            SelectionChanged="FoundFiles_SelectionChanged"
            HorizontalContentAlignment="Stretch">
            <ListBox.Resources>
                <Style TargetType="ListBoxItem">
                    <EventSetter Event="MouseDoubleClick" Handler="ListBoxItem_MouseDoubleClick" />
                </Style>
            </ListBox.Resources>
        </ListBox>

        <Label
            Grid.Column="1"
            Grid.Row="0"
            HorizontalAlignment="Center">
                Location
        </Label>
        <TextBox 
                Grid.Column="1"
                Grid.Row="1"
                HorizontalContentAlignment="Stretch"
                x:Name="SearchLocation"/>
        
        <StackPanel 
            Width="auto" 
            Height="auto"
            Grid.Row="2" 
            Grid.Column="1">

            <Label HorizontalAlignment="Center">
                Formats
            </Label>
            
            <ItemsControl 
                ItemsSource="{Binding FileTypeOptions}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox
                            Content="{Binding FileType}"
                            IsChecked="{Binding IsChecked, Mode=TwoWay}"
                            Margin="6"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" x:Name="btnPickLocation" Margin="5" Click="btnPickLocation_Click">Pick location</Button>
                <Button Grid.Column="1" x:Name="btnScan" Margin="5" Click="ButtonSearch_Click">Scan for images</Button>
            </Grid>
            
            <ProgressBar 
                x:Name="SearchProgressBar"
                Visibility="Hidden"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                IsIndeterminate="True"
                Width="300"
                Height="20"
                Margin="5"/>
            
            <Image
                x:Name="PreviewImage"
                Source="{Binding PreviewSource}"
                ToolTip="{Binding PreviewToolTip}"
                Stretch="Uniform"
                Width="200"
                Height="240"
                Margin="10"/>


        </StackPanel>
    </Grid>
</Window>
