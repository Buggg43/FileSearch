<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Search Images" Height="450" Width="800"
        Background="Wheat">
        
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Label HorizontalAlignment="Center">Files found</Label>
        <ListBox Grid.Row="1" x:Name="FoundFiles" ItemsSource="{Binding Files}" DisplayMemberPath="Name" SelectionChanged="FoundFiles_SelectionChanged" />

        <StackPanel 
            Width="auto" 
            Height="auto"
            Grid.Row="1" 
            Grid.Column="1" 
            Margin="5,0,0,0">
            
            <Label HorizontalAlignment="Center">
                Formats
            </Label>
            
            <ItemsControl ItemsSource="{Binding FileTypeOptions}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox
                            Content="{Binding FileType}"
                            IsChecked="{Binding IsChecked, Mode=TwoWay}"
                            Margin="6"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Button x:Name="btnAdd" Margin="5,0,0,0" Click="ButtonSearch_Click">Scan for images</Button>
            <Image
                x:Name="PreviewImage"
                Width="200"
                Height="250"
                Margin="10"/>

            <ProgressBar 
                x:Name="SearchProgressBar"
                Visibility="Hidden"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                IsIndeterminate="True"
                Width="300"
                Height="20"
                Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
